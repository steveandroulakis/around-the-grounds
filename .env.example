# Environment Configuration for Around the Grounds
# Copy this file to .env and customize for your deployment

# ========================================
# Temporal Configuration
# ========================================

# Temporal server address
# For local development (default):
# TEMPORAL_ADDRESS=localhost:7233

# For Temporal Cloud:
# TEMPORAL_ADDRESS=your-namespace.acct.tmprl.cloud:7233

# For custom Temporal server:
# TEMPORAL_ADDRESS=your-server.example.com:7233

# Temporal namespace (default: default)
# TEMPORAL_NAMESPACE=default

# Task queue name (default: food-truck-task-queue)
# TEMPORAL_TASK_QUEUE=food-truck-task-queue

# ========================================
# Authentication Configuration
# ========================================

# Option 1: API Key Authentication (Temporal Cloud)
# TEMPORAL_API_KEY=your-api-key-here

# Option 2: mTLS Authentication (Certificate-based)
# TEMPORAL_TLS_CERT=path/to/your/cert.pem
# TEMPORAL_TLS_KEY=path/to/your/key.pem

# Note: Use only ONE authentication method at a time
# - For local development: Leave all auth settings commented out
# - For Temporal Cloud: Use TEMPORAL_API_KEY
# - For custom servers: Use TEMPORAL_TLS_CERT and TEMPORAL_TLS_KEY

# ========================================
# Food Truck Application Configuration
# ========================================

# Claude Vision API for food truck name extraction
# ANTHROPIC_API_KEY=your-anthropic-api-key

# Vision analysis settings
# VISION_ANALYSIS_ENABLED=true
# VISION_MAX_RETRIES=2
# VISION_TIMEOUT=30

# ========================================
# Development Configuration Examples
# ========================================

# Example 1: Local Development (default)
# No configuration needed - just run the worker

# Example 2: Temporal Cloud
# TEMPORAL_ADDRESS=my-namespace.acct.tmprl.cloud:7233
# TEMPORAL_NAMESPACE=my-namespace
# TEMPORAL_API_KEY=abcdef1234567890

# Example 3: Custom Server with mTLS
# TEMPORAL_ADDRESS=temporal.mycompany.com:7233
# TEMPORAL_NAMESPACE=production
# TEMPORAL_TLS_CERT=/path/to/client.pem
# TEMPORAL_TLS_KEY=/path/to/client.key

# ========================================
# Production Deployment Notes
# ========================================

# 1. Never commit the actual .env file to version control
# 2. Store sensitive values (API keys, certificates) securely
# 3. Use environment variables in production instead of .env files
# 4. Test connectivity with a simple workflow before production deployment
# 5. Monitor worker logs for connection issues
# 6. Set up proper logging and alerting for production workers

# ========================================
# Troubleshooting
# ========================================

# If you encounter connection issues:
# 1. Check that TEMPORAL_ADDRESS is correct and accessible
# 2. Verify API key or certificate files are valid
# 3. Ensure network connectivity to Temporal server
# 4. Check worker logs for detailed error messages
# 5. Test with a simple local setup first

# For more help, see:
# - around_the_grounds/temporal/README.md
# - TEMPORAL-CLOUD-PLAN.md
# - CLAUDE.md (Temporal integration section)
name: Build and Push Docker Image

on:
  push:
    branches: [ main ]

env:
  GAR_LOCATION: us-central1
  GCP_PROJECT_ID: event-curation
  GAR_REPOSITORY: around-the-grounds
  IMAGE_NAME: app

permissions:
  contents: read
  id-token: write

jobs:
  build-and-push:
    # Scoped to jredding's fork so Steve's fork (and any third-party fork) shows
    # this job as "Skipped" in the Actions UI instead of failing on missing secrets.
    # Companion workflow: docker-build-push-dockerhub.yml (Steve's Docker Hub path).
    if: github.repository == 'jredding/around-the-grounds-brooklyn'
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: Authenticate to Google Cloud
      id: auth
      if: github.event_name != 'pull_request'
      uses: google-github-actions/auth@v2
      with:
        workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
        service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}
        token_format: access_token

    - name: Login to Artifact Registry
      if: github.event_name != 'pull_request'
      uses: docker/login-action@v3
      with:
        registry: ${{ env.GAR_LOCATION }}-docker.pkg.dev
        username: oauth2accesstoken
        password: ${{ steps.auth.outputs.access_token }}
    
    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.GCP_PROJECT_ID }}/${{ env.GAR_REPOSITORY }}/${{ env.IMAGE_NAME }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=raw,value=latest,enable={{is_default_branch}}
    
    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        platforms: linux/amd64,linux/arm64
        push: ${{ github.event_name != 'pull_request' }}
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
    
    - name: Image digest
      run: echo ${{ steps.build.outputs.digest }}
